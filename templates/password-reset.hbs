<!DOCTYPE html>
<html lang="{{__meta.locale}}" dir="{{__meta.direction}}">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{t "passwordReset.title"}}</title>
    {{> styles}}
</head>

<body>
    <div class="container">
        <center>
            {{> header}}

            <!-- Main Card -->
            <table width="600" cellpadding="0" cellspacing="0" class="main-card">
                <tr>
                    <td>
                        <h2
                            style="font-size: 20px; color: #333333; font-weight: 700; margin: 0 0 20px; line-height: 1.3;">
                            {{t "passwordReset.title"}}
                        </h2>

                        <p style="margin: 0 0 20px; color: #333333; font-size: 14px;">{{t "passwordReset.description"}}
                        </p>

                        <!-- Button -->
                        {{> button url=(or resetUrl '#') text=(t "passwordReset.ctaButton") }}

                        <div style="margin-top: 25px; padding-top: 20px; border-top: 1px solid #EEEEEE;">
                            <p style="margin: 0 0 10px; color: #E65100; font-size: 14px;">
                                <strong>{{t "passwordReset.warningLabel"}}</strong> {{t "passwordReset.warning"}}
                            </p>
                            <p style="margin: 0 0 10px; color: #666666; font-size: 13px;">
                                {{t "passwordReset.expiryInfo" hours=(or expiryHours 24)}}
                            </p>
                            <p style="margin: 0; color: #666666; font-size: 13px;">
                                {{t "passwordReset.ignoreNotice"}}
                            </p>
                        </div>
                    </td>
                </tr>
            </table>

            {{> footer}}
        </center>
    </div>
</body>

</html>
